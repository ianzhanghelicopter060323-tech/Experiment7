# template1.cpp 要求
编写函数模板，能够求出某数组中的最小值，并写主函数测试。

# template2.cpp 要求
编写函数模板，能够为某数组按照升序排序

# temp_class.cpp
编写一个模板类，能够存放大小为MAXSIZE的任意数据，类的功能包含：初始化、插入一个数据、读取一个数据，打印一个数据，对存放的数据排序等功能。思考：如果要能存放一个自定义类型的数据，对这个自定义类型有什么要求，可以自己尝试写写看哦~